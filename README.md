# Rust Sandbox

## Building all

```bash
cargo build --workspace
# or shorthand
cargo build -p '*'   
```

## Running a crate

```bash
# Example
cargo run -p rust-main
```

## Test a crate 

```bash
cargo run -p rust-simulator
```// test
// test again
